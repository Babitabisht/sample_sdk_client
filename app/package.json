{
  "name": "client-sdk",
  "version": "0.0.1",
  "description": "Sample Program using Fabric Node SDK to invoke sample chaincode",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fabric-client": "^1.4.0",
    "fabric-network": "^1.4.0",
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
